# itsme

송화영 개인 사이트. 정적 HTML만 있고 빌드 도구·의존성은 없다. 파일을 브라우저로 바로 열면 그대로 동작해야 한다.

- `index.html` — 포트폴리오 랜딩 페이지
- `weight.html` — 매일 아침 체중 기록 (index에서 링크하지 않는 비공개용 페이지, `noindex`)
- `meal.html` — 음식 사진 분석 결과 기록 (동일하게 비공개·`noindex`)

## 말투 규칙 (최우선)

이 저장소에서 사용자에게 답할 때는 **항상 존댓말**을 쓴다. 짧게 답할 때도 예외 없다. 반말 금지.

## 매일 아침 체중 입력 루틴

사용자가 아침에 숫자만 던지면 아래를 **그대로** 수행한다. 되묻지 말 것.

**숫자 해석 규칙**
- **4자리 숫자는 체중이다.** 앞 두 자리가 정수부, 뒤 두 자리가 소수부.
  `7374` → `73.74` kg, `6805` → `68.05` kg
- `72.4`, `오늘 71.8kg` 처럼 소수점·단위를 붙여 주면 그대로 쓴다.
- 값은 받은 정밀도 그대로 저장한다. 페이지도 소수 **둘째** 자리까지 그대로 표시한다.

1. `weight.html` 상단의 `<script id="weight-data">` JSON 블록 안 `entries` 배열에 한 줄 추가
   ```json
   {"d":"2026-08-04","w":73.74}
   ```
   - `d` = **오늘 날짜**(YYYY-MM-DD, 시스템 컨텍스트의 currentDate 기준)
   - `w` = 위 규칙으로 해석한 체중(kg)
   - 사용자가 한마디 덧붙이면(“어제 회식”, “운동함”) `"memo":"…"` 로 같이 기록
   - 같은 날짜가 이미 있으면 새로 추가하지 말고 그 줄을 덮어쓴다
   - 날짜 오름차순 유지 (페이지가 정렬하긴 하지만 diff 가독성을 위해)
   - **반드시 Edit로 해당 줄만 고친다. `weight.html` 전체를 Write로 다시 쓰지 말 것** —
     기록이 페이지 안에 들어 있어 전체 재작성은 데이터 유실 위험이 있다.
     정상적인 하루치 커밋은 `2 insertions(+), 1 deletion(-)` 이다(직전 줄에 쉼표가 붙어 -1).
     이보다 크게 바뀌었으면 커밋 전에 `git diff` 로 확인한다.
2. 커밋 메시지: `Log weight YYYY-MM-DD: 73.74kg`
3. `git push -u origin claude/daily-weight-tracking-trm7fj`
4. **현황 카드 이미지를 만들어 채팅에 첨부한다** (매번, 빠뜨리지 말 것)
   ```bash
   ln -sfn "<스크래치패드>/node_modules" node_modules   # playwright 없으면 스크래치패드에 npm i playwright
   node tools/shot.js "<스크래치패드>/weight-card.png"
   ```
   나온 PNG를 `SendUserFile` 로 보낸다 (`display:"render"`). `weight.html` 상단(제목·지표·그래프)을 그대로 잘라낸 이미지라 페이지와 항상 같은 내용이다.
5. 답장은 짧게, **존댓말로** — 기록 완료 + 전일 대비 / 7일 평균 정도만. 장황한 코칭 금지.

목표 체중·키는 `config.target` / `config.height` 에 넣으면 목표선과 BMI가 표시된다. 비워두면 해당 항목은 숨는다.

## 음식 사진 분석 루틴

### 손 기준자 (사용자 실측값)

사용자는 음식 사진에 **손이 함께 나오게** 찍는다. 아래는 직접 재서 알려준 값이다.

| 부위 | 실측 |
|---|---|
| 손 길이 (가운뎃손가락 끝 ~ 손목 접힘선) | **19 cm** |
| 손바닥 너비 (엄지 뺀 네 손가락 폭) | **9 cm** |
| 검지 길이 | **8 cm** |

사진에 손이 보이면 이 값으로 픽셀→cm 축척을 잡고, 그릇 지름·음식 면적을 환산해 양을 추정한다.
손이 없으면 그릇·수저 같은 일반적 크기 기준으로 추정하되 오차가 더 크다는 점을 감안한다.

**축척 잡는 법** (눈대중 말고 실제로 재라 — 눈대중은 2배까지 틀린다)

1. 사진을 색으로 분할한다. 살색 마스크와 음식 마스크를 만들고 `PIL`+`numpy` 로 화소를 센다.
   (`pip install pillow numpy` 필요. 마스크는 PNG로 저장해 **눈으로 확인할 것** — 손바닥을
   손가락으로 착각하는 실수를 여기서 잡는다.)
2. **손가락 하나의 최대 내접 폭**을 침식 반복 횟수로 잰다. 방향에 영향받지 않아 가장 안정적이다.
   붙어 있는 손가락은 쓰지 말고, 완전히 분리된 것만 쓴다.
3. 손바닥 9cm를 네 손가락에 배분한 표준값으로 환산한다 — **검지 2.3cm, 중지 2.3, 약지 2.2, 새끼 1.9**.
4. **서로 다른 손가락 두 개로 축척을 각각 구해 교차 검증한다.** 두 값이 5% 이상 벌어지면
   분할이 잘못된 것이니 마스크부터 다시 본다.

실측 예 (2026-08-26 계란): 검지 240px/2.3cm = 104.3, 새끼 200px/1.9cm = 105.3 px/cm (편차 1%).
계란 476×600px → 4.54×5.72cm → 특란(60~68g)으로 판정, 실제 특란 규격 4.45×5.8cm와 0.08cm 차이.

**주의**
- **배경이 살색과 비슷하면 분할이 안 된다.** 밝은 나무 책상이 대표적이다 — 실측해 보니
  손가락(R-G 26, Cb 108, Cr 143)과 책상(R-G 24, Cb 109, Cr 142)이 사실상 같은 값이었다.
  RGB·YCbCr 어느 쪽으로도 못 가른다. 이럴 땐 **기준자를 포기하고 일반 추정으로 가고,
  그 사실을 답장에 밝힌다.** 다음부터 손을 식판·접시처럼 대비되는 곳에 올려 달라고 안내한다.
- 손이 음식과 **같은 평면(테이블 위)** 에 있을 때만 기준자로 쓴다. 손이 들려 있거나 카메라 쪽으로
  나와 있으면 원근 때문에 축척이 틀어지므로, 그럴 땐 기준자를 쓰지 말고 일반 추정으로 돌아간다.
- 손으로 잡히는 건 **평면 치수(지름·길이)** 뿐이다. 높이·두께·국물 양은 여전히 보이지 않으므로
  손이 있어도 오차는 ±15% 안팎으로 남는다. 이보다 정밀한 것처럼 말하지 말 것.

### 시각 판정 — 사진이 오면 **먼저** 할 일

대화 중에는 시계가 없고, 서버는 UTC인데 사용자는 KST(UTC+9)다.
**시스템 컨텍스트의 currentDate 를 그대로 쓰면 아침에는 하루 어긋난다.**
그래서 사진이 오면 항상 촬영 시각부터 읽는다.

```bash
sh tools/phototime.sh "<업로드된 사진 경로>"   # → 2026-08-26 06:52:42  (KST)
```

이 결과의 **날짜를 `d` 에, 시각(HH:MM)을 `at` 에** 쓴다. 사진이 없는 체중 기록은
`date -u -d '+9 hours' +%F` 로 KST 오늘 날짜를 구한다.

### 남긴 음식 판정 규칙

먹기 전 사진만으로는 **차린 양**밖에 알 수 없다. 그대로 기록하면 남긴 만큼 매일 과대 계상된다.
판정은 아래 순서로 한다.

0. **먼저 "같은 것인지"부터 확인한다.** 시간만 보고 판단하지 말 것.
   - **용기를 대조한다.** 플라스틱 컵커피와 흰 종이컵은 다른 음식이다. 그릇 모양·색·무늬가
     다르면 남은 양이 아니라 **새 끼니**다.
   - **간격이 먹기에 너무 짧으면 다른 음식이다.** 19초 만에 250mL를 비울 수는 없다.
     이런 경우는 두 가지를 연달아 찍은 것이므로 각각 기록한다.
   - (실제 오류: 2026-08-26 컵커피 사진 19초 뒤 종이컵 사진을 남은 양으로 처리해 한 잔을 누락했다.)
1. **같은 음식 사진이 직전 기록의 `at` 로부터 1시간 이내에 오면 → 그것이 "남은 음식" 사진이다.**
   새 끼니를 만들지 말고 **직전 항목을 수정한다.** 두 사진을 비교해 먹은 비율을 정하고,
   `served`(차린 양 kcal)·`ate`(먹은 비율)를 붙이고 `kcal`·`c`·`p`·`f` 를 먹은 양으로 다시 계산한다.
   빈 그릇이면 `ate` 1.0 이므로 `served`·`ate` 를 지우고 원래대로 둔다.
2. **1시간이 지났거나 다른 음식이면 → 새 끼니**로 기록한다.
3. **말로 알려주면(“반 남김”, “국물만 남김”) 사진보다 그 말을 우선한다.**
   국물·밥은 남은 사진에서도 깊이가 안 보여서 사용자 체감이 더 정확하다.
4. **두 번째 사진이 오지 않으면 다 먹은 것으로 확정한다.** 남겼는지 캐묻지 말 것.
   다만 새로 기록한 끼니의 답장에는 "다 드신 걸로 기록했습니다 — 남기셨으면 알려주세요"를
   한 줄 덧붙여, 나중에라도 바로잡을 수 있게 한다.

`kcal`·`c`·`p`·`f` 는 항상 **실제 먹은 양**이다. 남긴 경우에만 `served`·`ate` 를 덧붙인다.
나중에 `ate` 만 고치면 전부 다시 계산된다.

```json
{"d":"2026-08-26","t":"점심","at":"12:30","n":"돈까스","kcal":616,"c":67,"p":21,"f":28,"served":880,"ate":0.7}
```

### 기록 절차

1. 사진을 보고 음식을 식별하고, 위 기준자로 양을 가늠해 열량·탄수화물·단백질·지방을 **추정**한다.
   남긴 양이 있으면 먼저 반영한 뒤 기록한다.
2. `meal.html` 의 `<script id="meal-data">` 안 `entries` 에 끼니마다 한 줄 추가 (Edit로만, 전체 재작성 금지)
   ```json
   {"d":"2026-08-20","t":"점심","n":"김치찌개, 흰쌀밥","kcal":680,"c":82,"p":28,"f":24}
   ```
   - `t` = 아침 / 점심 / 저녁 / 간식 (사진 시각이나 맥락으로 판단, 애매하면 사용자 표현 그대로)
   - `c`·`p`·`f` = 탄수화물·단백질·지방(g). 확신이 없으면 생략해도 페이지가 단색 막대로 처리한다.
   - `kcal` 은 `c*4 + p*4 + f*9` 와 크게 어긋나지 않게 맞춘다.
3. 커밋 메시지: `Log meal 2026-08-20: 점심 680kcal`
4. 커피·간식도 한 줄로 들어가므로 **"끼"가 아니라 "회"로 센다.** 페이지 표기도 "5회"다.
5. 답장에는 **추정치임을 한 번 밝힌다.** 양 추정 오차가 커서 실제 ±20~30%다.
   그릇·수저 같은 크기 기준이 안 보이면 오차가 더 크다는 점도 필요하면 알린다.
6. 확정적인 영양 조언·식단 처방은 하지 않는다. 기록과 추세만 전달한다.

목표 열량은 `config.goalKcal` 에 넣으면 목표선과 "목표 대비" 카드가 표시된다.

## 스타일 규칙

- 색·타이포는 `index.html`의 `:root` 토큰(ink / teal / slate / hair, Pretendard + JetBrains Mono)을 따른다.
- 외부 JS 라이브러리를 쓰지 않는다. 차트도 순수 SVG로 직접 그린다.
- 데이터는 별도 파일로 빼지 말고 HTML 안에 인라인으로 둔다 (`file://` 로 열어도 `fetch` 없이 동작하도록).
